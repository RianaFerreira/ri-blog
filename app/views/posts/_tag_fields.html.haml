%fieldset
  = f.label :name, 'Tag'
  = f.text_field :name

  - unless f.object.nil? || f.object.new_record?
    = f.hidden_field :_destroy
    = link_to 'Remove Tag', '#', class: 'remove_fields button'