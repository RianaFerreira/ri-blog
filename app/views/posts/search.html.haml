= title 'Search Results'

.row
  .small-11.small-centered.columns
    %h2 Search Results
    - @results.each do |r|
      %p
        %h4= r.title
        %span= r.created_at
      %p= r.detail
      -if r.image.present?
        %img{src: post.image, alt: 'blog post image'}